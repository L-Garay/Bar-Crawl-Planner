{
  "devDependencies": {
    "@testing-library/cypress": "^9.0.0",
    "@testing-library/dom": "^9.2.0",
    "@testing-library/react": "^14.0.0",
    "@types/jest": "^29.4.0",
    "concurrently": "^7.6.0",
    "cypress": "^12.9.0",
    "jest": "^29.4.3",
    "jest-mock-extended": "^3.0.1",
    "parcel": "^2.8.1",
    "ts-jest": "^29.0.5"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "moment-timezone": "^0.5.40"
  },
  "scripts": {
    "serverUnitTests": "cd server && npm run unitTests",
    "buildClient": "cd client && npm run build",
    "buildServer": "cd server && npm run generateAndCompile",
    "startClient": "cd client && npm run start",
    "startServer": "cd server && npm run startCompiled",
    "start": "concurrently \"npm run startClient\" \"npm run startServer\""
  },
  "engines": {
    "node": "^16.19.1"
  }
}
