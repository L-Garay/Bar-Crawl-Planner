overwrite: true
schema: "./src/schemas/schema.graphql"
generates:
  src/types/generated/graphqlTypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
