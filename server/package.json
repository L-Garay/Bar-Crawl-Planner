{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "dependencies": {
    "@apollo/server": "^4.0.1",
    "@prisma/client": "^4.5.0",
    "express": "^4.18.2",
    "graphql": "^16.6.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "generatePrismaSchema": "npx prisma generate --schema=./src/prisma/schema.prisma",
    "compile": "rimraf ./dist && tsc",
    "generateAndCompile": "npm run generatePrismaSchema && npm run compile",
    "start": "node ./dist/index.js",
    "compileAndStart": "npm run generateAndCompile && npm run start",
    "testPrismaQuerries": "npx ts-node prisma/testPrismaQuerries.ts",
    "postinstall": "prisma generate"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.2",
    "prisma": "^4.5.0",
    "ts-node": "^10.9.1"
  }
}
