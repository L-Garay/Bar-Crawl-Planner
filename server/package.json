{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "dependencies": {
    "@apollo/server": "^4.0.1",
    "@prisma/client": "^4.5.0",
    "express": "^4.18.2",
    "graphql": "^16.6.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "compile": "rimraf ./dist && npm run codegen && tsc",
    "generateAndCompile": "npx prisma generate && npm run compile",
    "start": "node ./dist/index.js",
    "compileAndStart": "npm run generateAndCompile && npm run start",
    "testPrismaQuerries": "npx ts-node src/prisma/testPrismaQuerries.ts",
    "postinstall": "prisma generate",
    "codegen": "graphql-codegen --config codegen.yml"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@graphql-codegen/cli": "2.13.8",
    "@graphql-codegen/typescript": "2.8.0",
    "@graphql-codegen/typescript-resolvers": "2.7.5",
    "@types/node": "^18.11.2",
    "prisma": "^4.5.0",
    "ts-node": "^10.9.1"
  },
  "prisma": {
    "schema": "./src/prisma/schema.prisma"
  }
}